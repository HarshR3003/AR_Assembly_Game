using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controller2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Chal rha hai!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
